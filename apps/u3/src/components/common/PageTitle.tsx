import styled from 'styled-components';

const PageTitle = styled.div`
  font-style: italic;
  font-weight: 700;
  font-size: 24px;
  line-height: 28px;

  color: #ffffff;
`;
export default PageTitle;
